# Токен бота от BotFather
BOT_TOKEN=your_bot_token_here

# ID администраторов (через запятую)
ADMIN_IDS=123456789,987654321

# Путь к директории с конфигурацией WireGuard (Liberty)
VPN_CONFIG_DIR=/opt/docker/liberty/config/wg

# Путь к директории с docker-compose (Liberty)
DOCKER_COMPOSE_DIR=/opt/docker/liberty

# Путь к SQLite БД клиентов (по умолчанию: clients.db в директории бота)
# DB_PATH=/opt/docker/liberty/telegram-bot/clients.db

# Префикс имён клиентов (например vpn_); пусто — без префикса
# CLIENT_NAME_PREFIX=vpn_

# Начальный IP адрес для клиентов (последний октет, обычно 2, так как 1 занят сервером)
VPN_CLIENT_START_IP=10

# DNS серверы для клиентов (через запятую)
DNS_SERVERS=1.1.1.1,8.8.8.8

# Имя интерфейса WireGuard (обычно wg0)
WG_INTERFACE=wg0

# Имя внешнего сетевого интерфейса для получения IP адреса
EXTERNAL_IF=eth0

# --- Xray (VLESS Reality): опционально, для выдачи VLESS-ссылок и QR ---
# Если не заданы, бот читает из DOCKER_COMPOSE_DIR/.install_info
# XRAY_CONFIG_DIR=/opt/docker/liberty/config/xray
# XRAY_PUBLIC_KEY=
# XRAY_PORT=443
# XRAY_SERVER_NAME=example.com
# XRAY_SHORT_ID=
