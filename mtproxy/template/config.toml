# Минимальный конфиг mtg v2. Секрет подставляет mtproxy-admin.sh (после bind-to).
# Внутри контейнера прокси слушает 443; снаружи — свой host_port на пользователя.

bind-to = "0.0.0.0:443"
concurrency = 3
